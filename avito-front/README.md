# Avito Frontend - Система модерации объявлений

**Тестовое задание для позиции Frontend разработчика**

Современное SPA-приложение для модерации объявлений на платформе Avito. Система предоставляет инструменты для эффективной работы модераторов: фильтрация, поиск, массовые операции, детальный просмотр объявлений и аналитика работы.

## 📋 Содержание

- [Описание проекта](#-описание-проекта)
- [Запуск проекта](#-запуск-проекта)
- [Стек технологий](#-стек-технологий)
- [Архитектура](#-архитектура)
- [Особенности реализации](#-особенности-реализации)

---

## 📖 Описание проекта

Система модерации объявлений — это инструмент для эффективной работы модераторов платформы Avito. Приложение позволяет просматривать, фильтровать и модерировать объявления, а также анализировать статистику работы модератора.

---

## ✅ Реализованные функциональные требования

### 1. Главная страница — Список объявлений (`/list`)

#### ✅ Отображение списка объявлений
- ✅ **Карточки объявлений** с информацией:
  - ✅ Изображение товара (первое фото из галереи)
  - ✅ Название объявления
  - ✅ Цена
  - ✅ Категория
  - ✅ Дата создания
  - ✅ Статус (на модерации / одобрено / отклонено / черновик)
  - ✅ Индикатор приоритета (обычный / срочный)

#### ✅ Фильтрация и поиск
- ✅ **Фильтр по статусу** (множественный выбор через select)
- ✅ **Фильтр по категории** (выпадающий список)
- ✅ **Фильтр по диапазону цен** (мин/макс)
- ✅ **Поиск по названию объявления** (с debounce)
- ✅ **Сброс всех фильтров** (кнопка "Очистить фильтры")

#### ✅ Сортировка
- ✅ **По дате создания** (новые/старые)
- ✅ **По цене** (возрастание/убывание)
- ✅ **По приоритету**
- ✅ **Порядок сортировки** (по возрастанию/убыванию)

#### ✅ Пагинация
- ✅ **10 объявлений на страницу**
- ✅ **Навигация между страницами** (предыдущая/следующая + номера страниц)
- ✅ **Отображение общего количества** объявлений

---

### 2. Страница детального просмотра объявления (`/item/:id`)

#### ✅ Информация об объявлении
- ✅ **Галерея изображений** (слайдер с минимум 3 изображениями, Swiper)
- ✅ **Полное описание** товара
- ✅ **Характеристики товара** (таблица ключ-значение)

#### ✅ Информация о продавце
- ✅ **Имя продавца**
- ✅ **Рейтинг** (звезды)
- ✅ **Количество объявлений**
- ✅ **Дата регистрации**

#### ✅ История модерации
- ✅ **Список всех действий** с объявлением
- ✅ **Кто проверял** (имя модератора)
- ✅ **Когда** (дата и время)
- ✅ **Какое решение принял** (одобрено/отклонено/на доработку)
- ✅ **Комментарий** (если был)

#### ✅ Панель действий модератора
- ✅ **Кнопка "Одобрить"** (зелёная)
- ✅ **Кнопка "Отклонить"** (красная)
- ✅ **Кнопка "Вернуть на доработку"** (жёлтая)

#### ✅ При отклонении
- ✅ **Обязательное поле для указания причины**
- ✅ **Быстрые шаблоны причин:**
  - ✅ Запрещённый товар
  - ✅ Неверная категория
  - ✅ Некорректное описание
  - ✅ Проблемы с фото
  - ✅ Подозрение на мошенничество
  - ✅ Другое (с полем ввода комментария)

#### ✅ Навигация
- ✅ **Кнопка "Назад к списку"**
- ✅ **Кнопки "Предыдущее" / "Следующее"** объявление (для быстрой модерации)

---

### 3. Страница статистики модератора (`/stats`)

#### ✅ Общая статистика
- ✅ **Карточки с метриками:**
  - ✅ Всего проверено объявлений (за сегодня/неделю/месяц/все время)
  - ✅ Процент одобренных
  - ✅ Процент отклоненных
  - ✅ Среднее время на проверку одного объявления

#### ✅ Графики
- ✅ **График активности по дням** (столбчатая диаграмма, Recharts)
- ✅ **Круговая диаграмма распределения решений** (одобрено/отклонено/на доработку)
- ✅ **График по категориям** проверенных объявлений

---

## ⭐ Реализованные дополнительные возможности (со звёздочкой)

### ✅ 1. Bulk-операции (массовые операции)
- ✅ **Чекбоксы** для выбора нескольких объявлений
- ✅ **Массовое одобрение** выбранных объявлений
- ✅ **Массовое отклонение** с указанием причины
- ✅ **Счетчик выбранных объявлений**
- ✅ **Тулбар** с действиями над выбранными объявлениями

### ✅ 2. Тёмная тема
- ✅ **Переключатель темы** (light/dark/system)
- ✅ **Сохранение выбора в localStorage**
- ✅ **Поддержка системной темы** (prefers-color-scheme)
- ✅ **Плавный переход** между темами

### ✅ 3. Экспорт данных (статистика)
- ✅ **Экспорт статистики в CSV** (таблица с метриками)
- ✅ **Генерация PDF-отчёта** (с графиками и визуализацией)

### ✅ 4. Фильтр по периоду на странице статистики (`/stats`)
- ✅ **Сегодня**
- ✅ **Последние 7 дней**
- ✅ **Последние 30 дней**
- ✅ **Все время**

### ✅ 5. Горячие клавиши (Keyboard Shortcuts)
- ✅ **A** - одобрить объявление (на странице `/item/:id`)
- ✅ **D** - отклонить объявление (открывает диалог отклонения)
- ✅ **→** (стрелка вправо) - следующее объявление
- ✅ **←** (стрелка влево) - предыдущее объявление
- ✅ **/** (слэш) - фокус на поле поиска (на странице `/list`)


### ✅ 6. UI/UX улучшения
- ✅ **Skeleton screens** (анимация загрузки вместо спиннеров)
- ✅ **Toast notifications** (уведомления об успехе/ошибке операций)
---

## ❌ Не реализованные дополнительные возможности

### ❌ 1. Продвинутая фильтрация
- ❌ Сохранение наборов фильтров (presets)
- ❌ URL-синхронизация фильтров (query params)


### ❌ 2. Real-time обновления
- ❌ Автообновление списка новых объявлений
- ❌ Счётчик новых объявлений
- ❌ WebSocket подключение


### ⚠️ 3. Анимации (частично реализовано)
- ✅ Skeleton screens (анимация загрузки)
- ✅ Progress bar для загрузки
- ❌ Плавные переходы между страницами (page transitions)
- ❌ Анимация появления карточек (staggered animation)

---

## 🚀 Запуск проекта

### Вариант 1: Запуск с Docker (рекомендуется)

Docker автоматически поднимет и фронтенд, и бэкенд в изолированных контейнерах.

#### Шаг 1: Проверьте установку Docker

```bash
docker --version
docker-compose --version
```

Если Docker не установлен, скачайте его с [официального сайта](https://www.docker.com/get-started).

**Минимальные требования:**
- Docker 20.10+
- Docker Compose 2.0+

#### Шаг 2: Клонируйте репозиторий

```bash
git clone <URL_репозитория>
```

#### Шаг 3: Перейдите в папку проекта

```bash
cd avito-front
```

#### Шаг 4: Запустите приложение

**Запуск в фоновом режиме (рекомендуется):**
```bash
docker-compose up -d
```

**Или запуск с выводом логов в терминал:**
```bash
docker-compose up
```

Docker Compose автоматически:
- Скачает необходимые образы (node:20-alpine, nginx:alpine)
- Соберет бэкенд и фронтенд
- Создаст изолированную сеть `avito-network`
- Запустит оба сервиса

#### Шаг 5: Откройте приложение в браузере

Приложение будет доступно по адресу:
- **Фронтенд**: http://localhost:8080
- **API сервер** (опционально): http://localhost:3002

**Важно:** Фронтенд обращается к API через внутреннюю Docker сеть (`http://server:3001`). Внешние порты 8080 и 3002 выбраны, чтобы избежать конфликтов с локальными сервисами.

---



### Вариант 2: Локальная разработка (без Docker)

Для разработки с hot-reload удобнее запускать проект локально.

#### Шаг 1: Проверьте установку Node.js и npm

```bash
node --version
npm --version
```

**Минимальные требования:**
- Node.js 20+
- npm 9+

Если Node.js не установлен, скачайте его с [официального сайта](https://nodejs.org/) или установите через nvm:

```bash
# Установка nvm (Node Version Manager)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Установка Node.js 20
nvm install 20
nvm use 20
```

#### Шаг 2: Клонируйте репозиторий

```bash
git clone <URL_репозитория>
```

#### Шаг 3: Перейдите в папку проекта

```bash
cd avito-front
```

#### Шаг 4: Установите зависимости

```bash
npm install
```

Установка займет 1-2 минуты. npm скачает все зависимости из package.json.

#### Шаг 5: Запустите API сервер (бэкенд)

**Откройте НОВЫЙ терминал** и выполните:

```bash
# Перейдите в папку бэкенда (на уровень выше и в tech-int3-server)
cd ../tech-int3-server

# Установите зависимости бэкенда
npm install

# Запустите сервер
npm start
```

Бэкенд запустится на http://localhost:3001

**Оставьте этот терминал открытым!**

#### Шаг 6: Запустите фронтенд (в исходном терминале)

Вернитесь в терминал с папкой `avito-front` и выполните:

```bash
npm run dev
```

Vite dev server запустится на http://localhost:5173 (или другой порт, если 5173 занят).

#### Шаг 7: Откройте приложение в браузере

Приложение будет доступно по адресу, который выведет Vite (обычно http://localhost:5173)


---

## 🛠 Стек технологий

### Обоснование выбора технологий

#### Core Framework

**React 19.2.0**
- ✅ Новейшие возможности React (improved hooks, concurrent features)
- ✅ Отличная экосистема библиотек и инструментов
- ✅ Виртуальный DOM для оптимальной производительности
- ✅ Идеально подходит для SPA с динамическим контентом

**TypeScript 5.9.3**
- ✅ Статическая типизация снижает количество ошибок
- ✅ Улучшенный developer experience (autocomplete, refactoring)
- ✅ Самодокументируемый код
- ✅ Обязательное требование для enterprise-приложений

**Vite 7.2.2**
- ✅ Мгновенный HMR (Hot Module Replacement) — в разы быстрее Webpack
- ✅ Нативная поддержка ES modules
- ✅ Оптимизированная production-сборка (Rollup под капотом)
- ✅ Минимальная конфигурация из коробки

#### State Management & Data Fetching

**TanStack Query (React Query) 5.90.8**
- ✅ Автоматическое кэширование и инвалидация данных
- ✅ Background refetching для актуальности данных
- ✅ Встроенная обработка loading/error состояний
- ✅ Optimistic updates для лучшего UX
- ✅ Упрощает код — не нужен Redux для серверного состояния

**Axios 1.13.2**
- ✅ Автоматическая трансформация JSON
- ✅ Interceptors для централизованной обработки ошибок
- ✅ Таймауты и отмена запросов
- ✅ Более удобный API по сравнению с fetch

#### Routing

**React Router DOM 7.9.5**
- ✅ Стандарт для SPA маршрутизации в React
- ✅ Nested routes и layouts
- ✅ Typed route parameters (с TypeScript)
- ✅ Программная навигация

#### UI 

**Radix UI + shadcn/ui**
- ✅ Полностью доступные компоненты (ARIA, keyboard navigation)
- ✅ Unstyled — легко кастомизировать под дизайн
- ✅ Маленький bundle size
- ✅ shadcn/ui — готовые компоненты с Tailwind-стилями

**Lucide React 0.553.0**
- ✅ 1000+ иконок
- ✅ Консистентный дизайн
- ✅ Легковесные SVG
- ✅ Tree-shaking — только используемые иконки попадут в bundle

#### Data Visualization

**Recharts 3.4.1**
- ✅ Декларативный API на базе React компонентов
- ✅ Responsive charts из коробки
- ✅ Богатая библиотека типов графиков
- ✅ Кастомизация и анимации
- ⚠️ **Альтернативы:** D3.js (более гибкий, но сложнее), Chart.js (императивный)

#### Export & Documents

**jsPDF 3.0.3 + dom-to-image-more 3.7.2**
- ✅ Генерация PDF прямо в браузере
- ✅ Конвертация DOM-элементов в изображения для отчетов
- ✅ Не требует бэкенда для генерации отчетов
- ⚠️ **Компромисс:** Генерация в браузере нагружает клиент, но упрощает архитектуру

#### Notifications

**Sonner 2.0.7**
- ✅ Современный дизайн toast-уведомлений
- ✅ Accessibility из коробки
- ✅ Promise-based API для async операций
- ✅ Минимальный setup

#### Theme Management

**next-themes 0.4.6**
- ✅ Поддержка системной темы (prefers-color-scheme)
- ✅ Сохранение выбора пользователя (localStorage)
- ✅ Предотвращение FOUC (Flash of Unstyled Content)
- ✅ Независим от Next.js (несмотря на название)

#### Image Carousel

**Swiper 12.0.3**
- ✅ Мобильный-first с touch gestures
- ✅ Lazy loading изображений
- ✅ Множество эффектов и настроек
- ✅ Отличная производительность

---

## 🏗 Архитектура

### Общая архитектура приложения

```
┌─────────────────────────────────────────────────────────────┐
│                       Docker Compose                         │
│  ┌───────────────────┐         ┌─────────────────────────┐  │
│  │  Nginx Container  │────────▶│  Backend Container      │  │
│  │  (Frontend)       │  HTTP   │  (Express API)          │  │
│  │  Port: 8080       │         │  Port: 3001 (internal)  │  │
│  └───────────────────┘         └─────────────────────────┘  │
│         │                                                     │
│         └─────────────── avito-network ──────────────────────┘
│
└──────────────────────────┬──────────────────────────────────
                           │
                   User Browser (localhost:8080)
```

### Frontend архитектура (Feature-based + Layered)

```
src/
├── api/                      # API Layer (Data Access)
│   ├── services/             # API сервисы (бизнес-логика API)
│   │   ├── adsService.ts     # Операции с объявлениями
│   │   ├── statsService.ts   # Получение статистики
│   │   └── moderatorService.ts # Информация о модераторе
│   ├── types/                # TypeScript типы для API
│   └── axiosInstance.ts      # Настроенный HTTP клиент
│
├── hooks/                    # Custom Hooks (Business Logic)
│   ├── ads/                  # Хуки для работы с объявлениями
│   │   ├── useAdsList.ts     # Список с фильтрами
│   │   ├── useAdDetails.ts   # Детали объявления
│   │   ├── useApproveAd.ts   # Мутация одобрения
│   │   ├── useRejectAd.ts    # Мутация отклонения
│   │   ├── useRequestChanges.ts
│   │   ├── useBulkApprove.ts # Массовое одобрение
│   │   └── useBulkReject.ts  # Массовое отклонение
│   ├── stats/                # Хуки статистики
│   └── moderator/            # Хуки модератора
│
├── components/               # UI Components (Presentation)
│   ├── ads/                  # Компоненты объявлений
│   │   ├── AdCard/           # Карточка объявления
│   │   ├── AdsList/          # Список объявлений
│   │   ├── AdsFilters/       # Фильтры и поиск
│   │   ├── AdsBulkActions/   # Панель массовых операций
│   │   ├── ModeratorPanel/   # Панель действий модератора
│   │   ├── RejectReasonDialog/ # Диалог причины отклонения
│   │   ├── ModerationHistory/  # История модерации
│   │   ├── AdInformation/    # Детальная информация
│   │   ├── ImageSlider/      # Слайдер изображений
│   │   └── skeletons/        # Скелетоны загрузки
│   ├── stats/                # Компоненты статистики
│   │   ├── StatsSummaryCards/# Карточки метрик
│   │   ├── ActivityChart/    # График активности
│   │   ├── DecisionsChart/   # График решений
│   │   └── CategoriesChart/  # График категорий
│   └── ui/                   # Базовые UI компоненты (shadcn/ui)
│
├── routes/                   # Pages (Route Components)
│   ├── ListPage/             # Страница списка объявлений
│   ├── ItemPage/             # Страница детального просмотра
│   └── StatsPage/            # Страница статистики
│
├── layouts/                  # Layout Components
│   ├── MainLayout.tsx        # Корневой layout
│   ├── Header/               # Хедер с навигацией
│   └── Footer/               # Футер
│
├── utils/                    # Utility Functions
│   ├── badges.tsx            # Badge компоненты
│   ├── formatters.ts         # Форматирование (цена, дата)
│   ├── chartColors.ts        # Цвета для графиков
│   ├── exportStats.ts        # Экспорт в CSV
│   └── exportStatsToPDF.ts   # Экспорт в PDF
│
├── types/                    # Global TypeScript Types
├── lib/                      # Third-party library configs
└── main.tsx                  # Application Entry Point
```

### Архитектурные паттерны и принципы

#### 1. **Layered Architecture (Слоистая архитектура)**

```
┌─────────────────────────────────────┐
│  Presentation Layer (Components)    │ ← UI компоненты, только отображение
├─────────────────────────────────────┤
│  Business Logic Layer (Hooks)       │ ← Бизнес-логика, state management
├─────────────────────────────────────┤
│  Data Access Layer (API Services)   │ ← HTTP запросы, работа с API
└─────────────────────────────────────┘
```

**Преимущества:**
- ✅ Четкое разделение ответственности
- ✅ Легко тестировать слои изолированно
- ✅ Переиспользование логики
- ✅ Простая замена имплементации (например, API на mock)

#### 2. **Custom Hooks Pattern**

Вся бизнес-логика инкапсулирована в custom hooks:

```typescript
// hooks/ads/useAdsList.ts
export const useAdsList = (params?: AdsListParams) => {
    return useQuery({
        queryKey: ["ads", params],
        queryFn: () => adsService.getList(params).then((res) => res.data),
        placeholderData: (prev) => prev // Оптимистичная пагинация
    });
};
```

**Преимущества:**
- ✅ Компоненты остаются "глупыми" (презентационными)
- ✅ Легко переиспользовать логику
- ✅ Проще тестировать (хуки можно тестировать отдельно)

#### 3. **Repository Pattern для API**

API сервисы — это репозитории, абстрагирующие работу с API:

```typescript
// api/services/adsService.ts
export const adsService = {
    getList: (params) => axiosInstance.get('/ads', { params }),
    getById: (id) => axiosInstance.get(`/ads/${id}`),
    approve: (id) => axiosInstance.post(`/ads/${id}/approve`),
    // ...
}
```

**Преимущества:**
- ✅ Централизованная точка для всех API вызовов
- ✅ Легко добавить interceptors, логирование, retry logic
- ✅ Простая смена backend (изменения только в сервисах)

#### 4. **Component Composition**

Сложные компоненты составляются из более мелких:

```
ListPage
├── AdsFilters
├── AdsList
│   └── AdCard (× N)
├── AdsBulkActions
└── AdsPagination
```

**Преимущества:**
- ✅ Каждый компонент имеет одну ответственность
- ✅ Легко переиспользовать компоненты
- ✅ Проще поддерживать и тестировать

#### 5. **Централизованная обработка состояний**

TanStack Query управляет всем серверным состоянием:

```typescript
// Кэширование
queryKey: ["ads", params] // Автоматический кэш по ключу

// Автоматическая инвалидация
onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["ads"] });
}

// Optimistic updates
placeholderData: (prev) => prev
```

**Преимущества:**
- ✅ Нет дублирования данных
- ✅ Автоматическая синхронизация UI
- ✅ Меньше кода для управления состоянием

### Data Flow (Поток данных)

```
User Action
    │
    ▼
Component (onClick)
    │
    ▼
Custom Hook (useMutation)
    │
    ▼
API Service (adsService.approve)
    │
    ▼
Axios Instance → HTTP Request → Backend
    │
    ▼
Response
    │
    ▼
TanStack Query (cache + invalidate)
    │
    ▼
Component Re-render (новые данные)
    │
    ▼
UI Update + Toast Notification
```


---


### Принятые компромиссы и ограничения

#### 1. **Client-side PDF generation вместо server-side**

**Решение:** Использовать jsPDF + dom-to-image

**Плюсы:**
- ✅ Не нужен backend для генерации PDF
- ✅ Простота реализации
- ✅ Меньше нагрузки на сервер

**Минусы:**
- ⚠️ Генерация в браузере медленнее (особенно с графиками)
- ⚠️ Ограничения качества (зависит от разрешения экрана)
- ⚠️ Увеличение bundle size (+200KB)

**Обоснование:** Для тестового задания достаточно, в production лучше делать на бэкенде (например, Puppeteer)

#### 2. **TanStack Query вместо Redux**

**Решение:** Использовать TanStack Query для серверного состояния

**Плюсы:**
- ✅ Автоматическое кэширование
- ✅ Меньше boilerplate кода
- ✅ Встроенная обработка loading/error
- ✅ Background refetching

**Минусы:**
- ⚠️ Локальное состояние (UI state) требует отдельного решения
- ⚠️ Менее подходит для сложной бизнес-логики

**Обоснование:** 90% состояния — это серверные данные, для остальных 10% достаточно useState/Context

#### 3. **Vite вместо Webpack**

**Решение:** Использовать Vite

**Плюсы:**
- ✅ Мгновенный HMR (в 10-20 раз быстрее)
- ✅ Быстрый dev server
- ✅ Оптимизированная production сборка

**Минусы:**
- ⚠️ Менее зрелая экосистема плагинов
- ⚠️ Некоторые legacy библиотеки могут иметь проблемы

**Обоснование:** Для современных SPA Vite — оптимальный выбор

#### 4. **Пагинация без виртуализации списка**

**Решение:** Простая пагинация по 10 элементов

**Плюсы:**
- ✅ Простота реализации
- ✅ Подходит для большинства случаев

**Минусы:**
- ⚠️ Не оптимально для очень длинных списков (1000+ элементов)

**Обоснование:** Для модерации обычно просматривают небольшие партии, виртуализация была бы overengineering



---

## 🔧 API Endpoints (используемые)

### Объявления
- `GET /api/v1/ads` - Список объявлений с фильтрами
- `GET /api/v1/ads/:id` - Детали объявления
- `POST /api/v1/ads/:id/approve` - Одобрить объявление
- `POST /api/v1/ads/:id/reject` - Отклонить объявление
- `POST /api/v1/ads/:id/request-changes` - Запросить изменения
- `POST /api/v1/ads/bulk-approve` - Массовое одобрение
- `POST /api/v1/ads/bulk-reject` - Массовое отклонение

### Статистика
- `GET /api/v1/stats/summary` - Сводная статистика
- `GET /api/v1/stats/activity` - График активности
- `GET /api/v1/stats/decisions` - Статистика решений
- `GET /api/v1/stats/categories` - Статистика по категориям

### Модератор
- `GET /api/v1/moderators/me` - Информация о текущем модераторе

---
